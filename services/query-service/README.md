# query-service (reserved)

Go service for full-text + structured + time-range search and SSE live tail
(Log Query & Search bounded context — see `docs/architecture/overview.md`).

**Status:** reserved path. Built in **issue #8**. When it lands it becomes its
own Go module here and gets added to the `use` block in the repo-root `go.work`.
