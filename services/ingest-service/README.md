# ingest-service (reserved)

Go + Gin service for authenticated high-throughput log intake (Ingestion
bounded context — see `docs/architecture/overview.md`).

**Status:** reserved path. Built in **issue #6**. When it lands it becomes its
own Go module here (`go mod init github.com/bRRRITSCOLD/logalot/services/ingest-service`)
and gets added to the `use` block in the repo-root `go.work`.
