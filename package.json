{
  "name": "logalot",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hostable multi-tenant logging platform (monorepo root).",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1"
  }
}
