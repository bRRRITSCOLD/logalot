Build a multi-tenant logging platform in THIS repo. You have the `ai` plugin (agents: ux-designer, frontend-engineer, backend-engineer, systems-architect, data-architect, staff-engineer, project-manager) plus superpowers + the figma companion. I'm asleep — full permission to auto-approve tools, gh, branches, PRs, and squash-merges. Don't stop until you hit a real blocker or the guard caps.

Run /ai:deliver to drive the whole pipeline (frame → plan → architecture → data → build → finish), then let its build phase flow into /ai:orchestrate (autonomous-delivery loop: dispatch → staff-engineer review gate → squash-merge) once the work is decomposed into GitHub issues.

GOAL — multi-tenant logging platform:
- High-volume ingest, live tail, full-text + structured search, dashboards, alerting
- Retention: 30-day hot tier + cold tier
- Multi-tenancy isolation as a first-class architecture concern (data + auth)

LOCAL INFRA — docker for everything local, for now:
- mongodb, postgres, redis, rabbitmq via docker-compose
- AWS-local emulation: floci (https://github.com/floci-io/floci) — NOT localstack. Have systems-architect + backend-engineer read floci's README/docs FIRST and model the AWS-local setup around it. If floci can't cover a needed service, track it as an issue rather than silently swapping in localstack.

DESIGN (Figma — figma companion must be authed with write access):
- Design system SOURCE: https://www.figma.com/design/9N3v2ZGGo3McfSxOLfBPnC/Design-System?node-id=0-1
  → ux-designer creates/extends the design system IN Figma + extracts tokens, wires Code Connect.
- App screens: https://www.figma.com/design/bgxzUUUNlz149nkYYjh67x/desktop-tablet-mobile?node-id=0-1
  → frontend-engineer builds the app (responsive desktop/tablet/mobile) FROM the design system, not ad-hoc.

DISCIPLINE:
- All engineers follow the principle skills: TDD, DDD (strategic always, tactical when earned), pragmatic SOLID (DI + interfaces, no ceremony), DRY/KISS/YAGNI.
- Stack defaults: backend Go+Gin / Node+Fastify / Rust+Axum (ports-and-adapters); frontend TanStack Start + Router + Tailwind/cva + TanStack Form + nuqs + zod@^4. Test tiers + Subject_Scenario_Expectation naming per the skills.
- git-workflow: small cohesive revertible PRs, Conventional Commits, squash-merge, restart off main.

AUTONOMY GUARDS:
- Every PR through the staff-engineer review gate before merge. One fix pass on non-approval, then flag open and move on.
- Write a durable progress ledger (.superpowers/delivery-progress.md) + GitHub issues as you go — they're the resume map if you crash or compact.
- Run the handoff skill before context gets large; prefer fresh sessions between big chunks.

REALISTIC TARGET for tonight (don't fake "done"): architecture + ADRs decided, data model + multi-tenant boundaries designed, issues decomposed/sequenced/tracked, docker infra scaffolded, design system + tokens in Figma, and the first vertical slice (ingest → store → live tail) built, reviewed, merged. Leave a clear morning status: what's merged, what's open, what's blocked.
