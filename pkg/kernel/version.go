package kernel

// Version identifies the shared kernel contract surface. Bump the minor when the
// published-language types or port signatures change in a backward-compatible
// way, and the major on a breaking change, so services can pin a contract.
const Version = "0.1.0"
