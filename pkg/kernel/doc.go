// Package kernel is the shared Go kernel for Logalot services.
//
// It is a *reserved placeholder* in this scaffold. The real kernel — the
// TenantContext, Principal, and the cross-service ports (LogStore, Broker,
// TailBus, ColdArchive, KeyStore, Authenticator) described in
// docs/architecture/overview.md — lands in issue #4.
//
// Everything here exists only so the Go workspace resolves and CI has a real
// module to build, lint, and test. Do not build service logic against it yet.
package kernel
